{
  "name": "insutech",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "npm install --prefix client && npm run build --prefix client && npm install --prefix server && npm run db:push --prefix server",
    "start": "npm start --prefix server"
  }
}
