{
  "name": "insutech",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "rm -rf client/node_modules server/node_modules && npm install --prefix client && npm run build --prefix client && npm install --prefix server && npx --prefix server prisma generate",
    "start": "npm run db:push --prefix server; npm start --prefix server"
  }
}
